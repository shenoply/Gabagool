const fs=require('fs'),vm=require('vm'),assert=require('assert');const THREE=require('../three-r128-build55.js');
const element=()=>({style:{},appendChild(){},textContent:''});const ctx={THREE,console,document:{hidden:false,createElement:element,body:element()},addEventListener(){},voiceOn:false,keys:{},joy:{x:0,z:0},root:new THREE.Group(),phase:'scavenge',carPanel77:undefined,wallPanel:null,tailButton67:element(),ui:{prompt:element()},$:()=>element(),sayToast(){},sfxCtx:()=>null,gameplayActive:()=>true,wallSolids:()=>[],resolveGeometry62(){},motion67:()=>'',control:()=>99,grab(){},ropeHints66(){},startScavenge(){},doJump(){},doBite(){},doRoll(){},whip67(){},tickWorld38(){},once67:()=>true};ctx.rat=new THREE.Group();vm.createContext(ctx);vm.runInContext(fs.readFileSync(require('path').join(__dirname,'../driving77.js'),'utf8'),ctx);
vm.runInContext(`
const model=new THREE.Group(),names=['Head','Spine','LeftUpLeg','RightUpLeg','LeftLeg','RightLeg','LeftArm','RightArm','LeftForeArm','RightForeArm'],bones={},rest={};for(const name of names){const b=new THREE.Bone();b.name=name;model.add(b);bones[name]=b;rest[name]=b.quaternion.clone();}const mixer=new THREE.AnimationMixer(model),idle=new THREE.AnimationClip('Idle',1,[new THREE.QuaternionKeyframeTrack('Head.quaternion',[0,1],[0,0,0,1,0,0,0,1])]);Object.assign(rat.userData,{pipMixer:mixer,pipBones:bones,pipRest:rest,pipActions:{Idle:mixer.clipAction(idle)}});drivingClips77(rat.userData);
car77={g:new THREE.Group(),owner:root,riding:false,speed:0,steer:0,horn:0,seat:new THREE.Vector3(0,1,0),wheel:new THREE.Group()};car77.g.position.set(0,0,20);rat.position.set(1,0,20);enterCar77();keys.w=true;for(let i=0;i<120;i++)controlCar77(1/60,{});this.result={riding:car77.riding,distance:car77.g.position.z-20,speed:car77.speed,clips:Object.keys(rat.userData.pipActions),loop:once67('Drive_Idle')};this.clipJSON=Object.values(rat.userData.pipActions).slice(1).map(a=>THREE.AnimationClip.toJSON(a.getClip()));
keys.w=false;keys.s=true;for(let i=0;i<300;i++)controlCar77(1/60,{});result.reverse=car77.speed<0;keys.s=false;for(let i=0;i<120;i++)controlCar77(1/60,{});exitCar77();result.exited=!car77.riding;result.scale=rat.scale.x;
`,ctx);assert(ctx.result.riding);assert(ctx.result.distance>5);assert(ctx.result.reverse);assert(ctx.result.exited);assert.equal(ctx.result.scale,1);assert.equal(ctx.result.loop,false);assert.equal(ctx.result.clips.length,5);for(const clip of ctx.clipJSON){assert(clip.tracks.every(t=>t.values.every(Number.isFinite)));}console.log(ctx.result);
